#include<iostream>
#include<vector>
using namespace std;

/**
 * arr中, 只有一种数出现过奇数次, 其他数都出现过偶数次, 找到他
 */
int get_odd_time_num1(vector<int> arr)
{
    int eor = 0;
    for (int num : arr)
    {
        cout << num << endl;
    }
}